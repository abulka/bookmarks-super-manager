declare const __APP_VERSION__: string

/**
 * "owner/repo" of the GitHub repo whose Releases feed drives the in-extension
 * self-update check. Empty string in the web build.
 */
declare const __UPDATE_REPO__: string